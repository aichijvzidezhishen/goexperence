# goexperence
## 算法与数据结构
### 
代理

## hw/base
1. 无缓冲 Chan 的发送和接收是否同步
2. go语言的并发机制以及它所使用的CSP并发模型
3. Golang 中常用的并发模型？
4. JSON 标准库对 nil slice 和 空 slice 的处理是一致的吗？
5. 闭包
6. 协程，线程，进程的区别
7. 互斥锁，读写锁，死锁问题是怎么解决(场景)
   自旋
   互斥量有两种模式：正常模式和饥饿模式。
    正常模式：正常模式下等待的协程按照先入先出排列，当一个协程被唤醒后并不是直接拥有锁，该协程需要和刚刚到达的协程一起竞争锁的所有权。新到的协程有个优势，那就是它已经在CPU上运行了，而且新到的协程可能有很多，所以被唤醒的协程极有可能抢占不到锁。在这种情况下，被唤醒的协程会被放置于等待队列的队头。如果等待的协程超过1ms内没有获取到锁，将会把锁置为饥饿模式。
    饥饿模式：在饥饿模式下，解锁的协程会将锁的所有权直接交给等待队列中位于队头的协程。正好解锁的那一刻有新的协程到达，新到达的协程也不会尝试自旋获取锁。相反，他们会将自己置于等待队列的队尾。
    如果等待队列中的协程获取到锁，它会查看
    (1)自己是否是等待队列中最后一个协程
    (2)自己等待的时间是否小于1ms
    如果有任意一个条件满足，将会将锁改为普通模式。
    一般认为普通模式会有更好的性能，因为即使有等待的协程，新的协程可以连续获取到锁。饥饿模式能够防止等待协程长时间获取不到锁。

1. Golang的内存模型（小对象多了会增加内存压力）
2.  slice传参有几种方式不改变原始数据
3.  epoll 、poll、select
4.  channel线程安全
5.  map 线程安全
6.  golang gc
7.  goroutine 调度
8.  并发和并行
9.  uintptr 和 unsafe.Pointer 的区别 
    uintptr 和 unsafe.Pointer 是 Go 语言中用于处理指针的类型，它们的使用场景和特性有所不同。

    uintptr 是一个无符号整数类型，用于存储指针的数值。它可以用于将指针转换为整数，以及将整数转换回指针。但是，uintptr 并不保留指针的类型信息，因此在将整数转换回指针时需要确保类型的正确性。uintptr 主要用于底层编程，例如在进行指针运算或与 C 语言进行交互时。

    unsafe.Pointer 是一个特殊的指针类型，可以用于存储任意类型的指针。它可以将指针转换为 unsafe.Pointer 类型，以及将 unsafe.Pointer 转换回原始指针类型。unsafe.Pointer 的使用需要谨慎，因为它可以绕过 Go 语言的类型系统，可能导致类型不匹配或内存安全问题。因此，它主要用于与 C 语言进行交互或进行底层编程时，例如在使用 cgo 包调用 C 函数时。

    总结来说，uintptr 用于将指针转换为整数，而 unsafe.Pointer 用于在指针和不同类型之间进行转换。在一般情况下，应尽量避免使用 uintptr 和 unsafe.Pointer，而是使用 Go 语言提供的类型安全的指针操作和类型转换机制。只有在必要的情况下，才应该使用 uintptr 和 unsafe.Pointer，并且需要谨慎处理类型和内存安全性。

10. 反向代理- 负载均衡器
11. goroutinue channel mutex 应用场景
12. log 线程安全么  
13. Golang 里的逃逸分析是什么？怎么避免内存逃逸？
14. 配置中心如何保证一致性？
15. Golang 的GC触发时机是什么?
16. Redis 里数据结构的实现熟悉吗?
17. Etcd的Raft一致性算法原理?

## 微服务架构
1. swarm选举过程
2. 关于任期
3. 关于随机超时
4. SLB原理
5. 分布式一致性原则
6. 如何保证服务宕机造成的分布式服务节点处理问题?
7. 服务发现怎么实现的
8. CI/CD
    基于 Docker 的 CI/CD 工具 Drone 所有编译、测试的流程都在 Docker 容器中进行。
    开发者只需在项目中包含 .drone.yml 文件，将代码推送到 git 仓库，Drone 就能够自动化的进行编译、测试、发布。
    持续交付/持续部署 
·   Trigger:
    event :
        - push 

   steps :
        build
        ...
        push 
        ...
        deploy
        ...

## docker + k8s
    镜像部署
    k9s 管理kubernetes集群工具
    pods
    images
    容器
    
## 数据库
### 一、mysql
1. 索引优化，如何查询优化结果
   explain 
2. 联合索引 怎么写？ 规则，生效时间
3. 索引原理 b+树
   未添加索引时执行全量搜索 
4. 事务隔离级别
5. 最左匹配原则
   
6. MySQL 的特点
7. MySQL 字段类型
    数值类型
    字符串类型：
        定长字符串： 
            
        变长字符串：
            
    日期时间类型

8. MySQL 存储引擎
   myisam
   innodb
9.  MySQL 事务

### 二、MySQL 高性能优化规范建议
1. 数据库命名规范
2. 数据库对象名称格式
3. 禁止使用保留关键字
4. 临时库表和备份表命名 
5. 相同数据的列名和类型一致性
### 三、数据库基本设计规范
1. 存储引擎选择
2. 字符集统一
3. 表和字段注释
4. 单表数据量控制
5. 分区表使用
6. 相关列放置
7. 预留字段和大二进制数据存储
8. 不被范式过度束缚
三、数据库字段设计规范
选择合适最小数据类型
避免某些数据类型
NOT NULL 的定义
日期和财务金额数据存储
单表字段数量控制
四、索引设计规范
索引数量限制
禁止的索引类型
主键设置
索引列顺序选择
冗余和重复索引避免
覆盖索引考虑
五、索引 SET 规范
外键约束使用
关联键索引建立
六、数据库 SQL 开发规范
数据库复杂运算避免
重要 SQL 语句优化
已有索引利用
避免的查询条件和操作
特定查询和插入语句使用
预编译语句使用
数据类型隐式转换避免
子查询、过多 JOIN 表、随机排序等避免
UNION ALL 使用
七、数据库操作行为规范
超百万行批量写操作
大事务和大表修改问题
禁止的权限

### mongodb
1. 事务 session   
2. 文档数据库 高性能、高可用、易扩展
3.  mongo 分片 解决系统增长：垂直拓展、水平拓展
    mongo的分片集群包含以下组件：
4. 后台创建索引
5. 副本集
6. 聚合查询
   -  $match:
        db.collection.aggregate([
            {
                $match:{age:{$gt:20}}
            }
        ]);
    - $group:
        db.collection.aggregate([
            {
                $goroup :{
                    _id : "$city",
                    totalUsers : {$sum:1}.
                }
            }
        ])
   -  sort,limit:
        db.collection.aggregate([
            {
                $sort:{age:-1}
            }
        ])
    
        db.collection.aggregate([
            { $sort: { age: -1 } },
            { $limit: 10 }
        ]);
    - match,limit 的顺序问题。 
        match 一般放在limit之前，可以先筛选文档，减少后续阶段的数据量，这样可以提高整个聚合操作的性能。 特别是当原始数据集很大时，能够显著降低后续$group 等阶段需要处理的数据量
    - skip 、 limit、sort 顺序问题
    - $project filter cond  
  
         ``` js
            $filter: {
                        input: "$baginfo.items",
                        as: "item",
                        cond: {
                            $in: ["$$item.dictid", {
                                $ifNull: ["$baseInfo.usemedallist", []]
                            }]
                        }
                }
        ```

7. 分片集群的结构
    shard
    mongos ：查询路由，提供客户端和分片集群之间的借口
    config servers ：存储元数据和配置数据
    分片键（ 必须是一个索引）
    选择分片键的原则：高基数 、经常用于查询和范围查询 

    集合中选择一个字段或者组合字段，用该键的值作为数据拆分的依据。
    
    注意事项：
        分片键是不可变。
        分片键必须有索引。
        分片键大小限制512bytes。
        分片键用于路由查询。
        MongoDB不接受已进行collection级分片的collection上插入无分片键的文档（也不支持空值插入）
        

    哈希分片（复合索引也可以进行哈希分片）
        分片过程中利用哈希索引作为分片，基于哈希片键最大的好处就是保证数据在各个节点分布基本均匀。

    范围分片（range sharding）
        将单个Collection的数据分散存储在多个shard上，用户可以指定根据集合内文档的某个字段即shard key来进行范围分片（range sharding）。
8. 数组字段查询
    * 过滤数组中不需要的字段 $not $in {{"items.id:  {$not {$in [1,2,3]}}}} 
    * $unwind 
    * $push  
    


### redis 
1. 事务性
2. 数据持久化方式
3. 缓存淘汰策略
4. [分布式锁实现](hw\redis-lock\lock.go)
    a.分布式锁 死锁怎么解决
        设置锁超时时间 + 重试次数
        采用的自动续租机制
        锁的检测  服务追踪：jager 服务追踪 ：链路追踪 tacer
            opentracing 数据模型
                Span ：链路基本单元
    
5. Pub/Sub模型
   可伸缩的异步消息系统
   
6. Redis 线程模型
   a. 引入多线程是为了提高网络ID读写性能 
   b. 开启多线程： io-threads（多线程写） 
   c. io-threads-do-reads yes (多线程读)
7. redis后台线程
   a. 释放AOF/RDB 过程中产生的临时文件资源
   b. 后台线程调用fsync函数 强制将缓冲区的数据落地到磁盘（AOF文件）
8.  过期策略
    a. 大量Key过期
        i.  尽量避免key集中过期，加随机时间戳。
        ii. 对过期的key开启惰性删除机制（lazyfree）-- 主动查询的时候会删除。
    b. 
        
9.  内存淘汰策略
10. redis 优化
     a. 批量操作 集群的时候要注意，确定要操作的key在同一个槽位
     b. lua 脚本不支持回滚。集群环境下 无法保证原子操作。
    1. 处理big key
    2. hot key（热key）


### PostgreSQL
1. MVCC机制
2. 索引优化
3. 事务隔离级别
4. 触发器和存储过程
5. 分区表

### Distributed Systems
1. CAP理论
2. 一致性哈希算法
3. Paxos和Raft算法
4. 分布式事务
5. 分布式锁
6. 服务发现和注册
7. 负载均衡策略
8. 分布式存储系统


## NetWork
1. 公网 内网 专用网络
2. 代理
3. websocket 属于网络分层那一层
4. https 流程是怎样的（公私钥、数字证书、加密、对称加密、非对称加密。）
5. 说说HTTP的状态码，301和302的区别？
6. 对称加密和非对称加密  
7. dns解析过程
8. csrf 攻击 如何避免
9.  websocket 和 socket 区别
10. DoS、DDoS、DRDoS攻击
11. xxs攻击
12. http请求的过程和原理
13. forward 和redirect 的区别
    直接转发方式 
    间接转发方式
14. sql 注入，如何攻击
15. 如何预防sql 注入问题
16. Session和Cookie的区别。
17. IP地址有哪些分类？
18. ARP 协议的工作过程
19. TCP 和 UDP 分别对应的常见应用层协议
20. 保活计时器
21. 服务器出现了大量CLOSE_WAIT状态如何解决
22.  URI和URL的区别
23.  ICMP协议的功能
24.  ping的原理
25.  TCP 的三次握手机制

    [![三次握手]](http://mianbaoban-assets.oss-cn-shenzhen.aliyuncs.com/xinyu-images/MBXY-CR-f795189dd1254b2b5765b0a617198b9c.png)
26. TCP四次挥手过程
    断开链接

27. TCP的粘包和拆包
28. 说说半连接队列和 SYN Flood攻击的关系
29. TCP的滑动窗口
    
30. TCP的拥塞控制
31. 慢启动算法
32. 拥塞避免算法
33. TCP报文首部有哪些字段，作用
    1. 16位端口号：源端口号，主机该报文段是来自哪里；目标端口号，要传给哪个上层协议或应用程序
    2. 32位序号：一次TCP通信（从TCP连接建立到断开）过程中某一个传输方向上的字节流的每个字节的编号。
    3. 32位确认号：用作对另一方发送的tcp报文段的响应。其值是收到的TCP报文段的序号值加1。
    4. 4位头部长度：表示tcp头部有多少个32bit字（4字节）。因为4位最大能标识15，所以TCP头部最长是60字节。
    5. 6位标志位：URG(紧急指针是否有效)，ACk（表示确认号是否有效），PSH（缓冲区尚未填满），RST（表示要求对方重新建立连接），SYN（建立连接消息标志接），FIN（表示告知对方本端要关闭连接了）
    6. 16位窗口大小：是TCP流量控制的一个手段。这里说的窗口，指的是接收通告窗口。它告诉对方本端的TCP接收缓冲区还能容纳多少字节的数据，这样对方就可以控制发送数据的速度。
    7. 16位校验和：由发送端填充，接收端对TCP报文段执行CRC算法以检验TCP报文段在传输过程中是否损坏。注意，这个校验不仅包括TCP头部，也包括数据部分。这也是TCP可靠传输的一个重要保障。
    8. 16位紧急指针：一个正的偏移量。它和序号字段的值相加表示最后一个紧急数据的下一字节的序号。因此，确切地说，这个字段是紧急指针相对当前序号的偏移，不妨称之为紧急偏移。TCP的紧急指针是发送端向接收端发送紧急数据的方法。

34.  超时重传
35.  重复SACK（D-SACK）
36.  http 常见的端口
    - 21:FTP（文件传输协议）
    - 22：ssh
    - 25:SMTP（简单邮件传输协议）
    - 53:DNS 域名服务器
    - 80:HTTP 超文件传输协议
    - 110：POP3 邮件协议3
    - 443:HTTPS
    - 1080:Sockets
    - 1521：Oracle 数据库默认端口
    - 3306：MYSQL 服务默认端口

## 操作系统
### 操作系统结构
1. 内核
2. 用户态和内核态
3. 用户态和内核态是如何切换

### 进程和线程
1. 并行和并发有什么区别
2. 进程上下文切换
    进程既可以在用户空间运行，又可以在内核空间运行。
    - 前后两个线程属于不同进程。此时，因为资源不共享，所以切换过程就跟进程上下文切换是一样。
    - 前后两个线程属于同一个进程。此时，因为虚拟内存是共享的，所以在切换时，虚拟内存这些资源就保持不动，只需要切换线程的私有数据、寄存器等不共享的数据
3. 进程有哪些状态
   就绪、运行、阻塞、创建、终止
4. 僵尸进程
5. 孤儿进程
6. 进程有哪些调度算法
7. 进程间通信有哪些方式
8. 进程和线程的联系和区别
   - 线程和进程的联系
   - 线程与进程的⽐较如下

9.  线程有哪些实现方式
    - 内核态线程实现
    - ⽤户态线程实现
    - 混合线程实现

10. 线程间如何同步
11. 死锁
12. 死锁产生有哪些条件？
13. 避免死锁
14. 活锁和饥饿锁

### 虚拟内存
1. 定义
2. 内存分段
3. 内存分页
4. 多级页表
5. 块表
6. 分页和分段
7. 交换空间
8. 页面置换算法
9. 硬连接和软连接的区别

### IO
1. 零拷贝
2. 阻塞与⾮阻塞 **I/O **、 同步与异步 I/O？
3. IO 多路复用
   select poll epoll


### 算法基础
## 数据结构
1. 二叉平衡树解决二叉树不平衡导致的性能问题，通过调整树的结构，来达到高度的平衡状态，但是频繁的调整树的结构也是一种开销，而红黑树就是舍去一部分平衡性来获得低的调整开销，但是红黑树也存在‘右倾现象’，查找性能又会降低。
    因此很难在查找性能和调整树的开销之间找到一种平衡，所以不适合做mysql的底层索引结构
2. 栈
3. 队列
    看下lru 结合redis
4. 二叉树、 满二叉树
   a. 平衡二叉树
   b. b树 b+树（mongo、mysql索引）
5. 链表


## 算法
 
1. 字符串之实现 Sunday 匹配
2. 字符串泄漏之反转字符串(301)
3. 字符串中的第一个唯一字符
4. 字符串之验证回文串
5. 滑动窗口最大值
6. 最长公共前缀
7. 两个数组的交集
8. 最接近的三数之和
9. Aoi 十字链表，灯塔，九宫格
    a. 插入  链表长度很长时，要从头开始遍历，效率低（可以用快排，分治，按范围划分）
    b. 删除  链表长度很长时，要从头开始遍历，效率低（可以用快排，分治，按范围

## linux 

### 常用命令
1. 目录切换  
2. 权限更改
3. 文件压缩
     tar
     zip
   
4. 用户管理
5. 系统状态查看
   (1) 查看系统进程信息
    ps aux |grep "redis"
  （2）杀死进程
    kill -9  12345
    kill -9 $(ps -ef | grep "redis" | grep -v grep | awk '{print $2}')
    kill all redis-server
  （3）查看端口占用
    netstat -tlnp | grep 6379
    lsof -i:6379
  （4）查看内存
    free -h
  （5）查看cpu
    top
  （6）查看磁盘
    df -h
  （7）查看系统版本
    lsb_release -a

